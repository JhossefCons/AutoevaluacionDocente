<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Realizar Autoevaluación</title>
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" integrity="sha384-******************************" crossorigin="anonymous">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .fondo{
            background-image: url(../img/fondo2.jpg);
            background-size: cover;
            background-repeat: no-repeat;
            margin-top: -46px;
            height: 1000px;
        }
        .form-group p{
        color: black;
        font-size: 17px;
    }

    </style>
</head>
<body>

    <div class="fondo">
        <div class="container mt-5">
            <div class="row">
                <h3 style="text-align: center; color: white; margin-top: 50px;">Formulario de Autoevaluación</h3>
                <div class="col-md-5" style="margin: auto; margin-top: 100px; margin-bottom: 100px;">
                    <div class="card">
                        <div class="card-body">
                            <form action="/docenteDiligenciarAutoevaluacion/<%= data.autoevaluation_id %>" method="post">
                                <table class="table">
                                    <tbody>
                                        <tr>
                                            <th>Identificación de Usuario</th>
                                            <td><%= data.user_identification %></td>
                                        </tr>
                                        <tr>
                                            <th>Código de Labor</th>
                                            <td><%= data.labor_code %></td>
                                        </tr>
                                        <tr>
                                            <th>ID del Período</th>
                                            <td><%= data.period_id %></td>
                                        </tr>
                                        <tr>
                                            <th>Acto</th>
                                            <td>
                                                <% if (data.acto === 'si') { %>
                                                    Si
                                                <% } else { %>
                                                    No
                                                <% } %>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Estado</th>
                                            <td><%= data.state %></td>
                                        </tr>
                                    </tbody>
                                </table>
                        </div>
                    </div>
                </div>
                <div class="col-md-7" style="margin: auto; margin-top: 100px; margin-bottom: 100px;">
                    <div class="card">
                        <div class="card-body">
                                <table class="table">
                                    <tbody>
                                        <tr>
                                            <th>Fecha de Inicio</th>
                                            <td><%= data && data.date_init ? data.date_init.toISOString().substring(0, 10) : '' %></td>
                                        </tr>
                                        <tr>
                                            <th>Fecha de Finalización</th>
                                            <td><%= data && data.date_finish ? data.date_finish.toISOString().substring(0, 10) : '' %></td>
                                        </tr>
                                        <tr>
                                            <th>Activo</th>
                                            <td>
                                                <% if (data.activo === 1) { %>
                                                    Activo
                                                <% } else { %>
                                                    Inactivo
                                                <% } %>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Resultados</th>
                                            <td>
                                                <textarea name="resultados" class="form-control" rows="4"><%= data.resultados %></textarea>
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>Evaluación</th>
                                            <td>
                                                <input type="number" name="evaluacion" value="<%= data.evaluacion %>" class="form-control" min="0" max="100">
                                            </td>
                                        </tr>
                                        <tr>
                                            <th>sugerencias</th>
                                            <td>
                                                <textarea name="sugerencias" class="form-control" rows="4"><%= data.sugerencias %></textarea>
                                            </td>
                                        </tr>
                                    </tbody>
                                </table>
                                <div>
                                    <button type="submit" class="btn btn-primary">
                                        Enviar
                                    </button>
                                    <a type="submit" href="/docenteDiligenciar" class="btn btn-secondary">Cancelar</a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <% if(typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>' ,
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmationButton: <%= showConfirmButton %>,
                timer:<%= timer %>
            }).then(()=>{
                window.location='/<%= ruta %>'
            })
        </script>
    <% } %>
</body>
</html>